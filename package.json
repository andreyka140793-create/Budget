{
  "name": "dzen-budget",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node --watch server/index.js",
    "start": "node server/index.js",
    "bot": "node bot/index.js",
    "build:web": "npm --prefix webapp install && npm --prefix webapp run build",
    "postinstall": "npm run build:web"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "grammy": "^1.30.0",
    "helmet": "^8.0.0"
  }
}
